---
"@moonshot-ai/kimi-code": minor
---

Add the /acp slash command to manage the ACP (Agent Client Protocol) context manager. Run /acp status to check health, /acp enable or /acp disable to switch it, or /acp reset to reset ACP state.
