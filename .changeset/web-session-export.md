---
"@moonshot-ai/kimi-code": minor
---

web: Add the `/export` slash command to download the current session and bounded metadata-only troubleshooting logs as a ZIP. Web downloads are limited to 64 MiB.
