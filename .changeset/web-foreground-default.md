---
"@moonshot-ai/kimi-code": minor
---

Run `kimi web` and the TUI `/web` command in the foreground by default instead of backgrounding a daemon; pass `--background` to keep the previous background behavior.
