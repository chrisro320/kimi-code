Report ACP context-manager state: whether ACP is active, sidecar health, context usage, and existing compression blocks.

Read-only; use it before planning compression ranges or after errors to check for degraded state.
